if [ $OS == 'darwin' ] ; then
    # Turn off the "The default interactive shell is now zsh." warning.
    export BASH_SILENCE_DEPRECATION_WARNING=1

    # Homebrew
    [ -f /opt/homebrew/bin/brew ] && eval "$(/opt/homebrew/bin/brew shellenv)"
fi
