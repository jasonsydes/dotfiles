# Local bin directory.
PATH=~/bin:$PATH

# The other local bin directory.
# Potentially used by many tools (currently just lvim prebuild binary on MGustave).
PATH=~/.local/bin:$PATH

# The opt local bin directory.
# Potentially used by many tools (currently just lvim built from source on longreads).
PATH=~/opt/bin:$PATH
